import {assetUrl} from './assetUrl';
import {Move3D,ArrowDownToLine} from 'lucide-react';
export const caseStudies=[{id:'bridge',number:'01',title:'Twin-Arch Web Bridge',short:'Bridge'},{id:'loop-towers',number:'02',title:'Loop Towers',short:'Loop Towers'},{id:'pavilion',number:'03',title:'Woven-Wing Pavilion',short:'Woven Pavilion'}];
export function Header({pipeline=false}:{pipeline?:boolean}){return <header className="platform-header"><a className="platform-brand" href={pipeline?"#/pipeline":"#/structural-lab"}><span className="platform-mark"><Move3D size={23}/></span><strong>CEPHALO-SILK <span className={pipeline?"pipeline-brand-label":undefined}>/ {pipeline?"DESIGN PIPELINE":"STRUCTURAL LAB"}</span></strong></a><span className="platform-flow">{pipeline?<>DESIGN <i>→</i> ANALYZE <i>→</i> FEEDBACK <i>→</i> REVISE</>:<>IMAGE <i>→</i> GEOMETRY <i>→</i> RESPONSE</>}</span>{['localhost','127.0.0.1','[::1]'].includes(location.hostname)?<span className="platform-download" title="Editable source is included in the extracted project folder">Source included</span>:<a className="platform-download" href={assetUrl("downloads/cephalo-structural-lab.zip")} download><ArrowDownToLine size={16}/><span>Source project</span></a>}</header>}
export function CaseNavigation({active}:{active:string}){return <nav className="case-nav" aria-label="Case studies"><span className="nav-label">CASE STUDIES</span>{caseStudies.map(c=><a key={c.id} href={`#/${c.id}`} aria-current={active===c.id?'page':undefined}><span>{c.number}</span>{c.short}</a>)}<small>Independent structural models</small></nav>}
