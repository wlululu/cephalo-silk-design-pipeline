import {assetUrl} from './assetUrl';
import {useRef} from 'react';
export default function ConceptCard({caseId,title}:{caseId:string,title:string}){const dialog=useRef<HTMLDialogElement>(null);return <><button className="concept-card" onClick={()=>dialog.current?.showModal()} aria-label="Enlarge Original Concept"><img src={assetUrl(`cases/${caseId}/original-concept.png`)} alt={`Original ${title} concept`}/><span>Original Concept <b>↗</b></span></button><dialog ref={dialog} className="concept-dialog" onClick={e=>{if(e.target===dialog.current)dialog.current.close()}}><button className="dialog-close" aria-label="Close Original Concept" onClick={()=>dialog.current?.close()}>×</button><span className="eyebrow">ORIGINAL CONCEPT</span><h2>{title}</h2><img src={assetUrl(`cases/${caseId}/original-concept.png`)} alt={`Original ${title} concept enlarged`}/><p>Original 2D concept → inferred 3D geometry → structural representation → mechanical response.</p></dialog></>}
